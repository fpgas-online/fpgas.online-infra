- name: Systemd pistat_ssh.service
  template:
    src: "files/{{ item }}.service"
    dest: "/etc/systemd/system/{{ item }}.service"
  notify: Reload-systemd
  with_items:
    - pistat_ssh
    - pistat_cam
    - pistat_info
    - pistat_shutdown
  tags:
    - pistat

- name: Enable pistat services
  systemd:
    name: "{{ item }}"
    enabled: true
  failed_when: false
  with_items:
    - pistat_ssh
    - pistat_cam
    - pistat_info
    - pistat_shutdown
  tags:
    - pistat
