---
- name: Add ssh keys
  become: true
  become_user: pi
  ansible.builtin.command: "ssh-import-id {{ item }}"
  with_items: "{{ ssh_imports | default([]) }}"
  when: ssh_imports is defined and ssh_imports | length > 0
  failed_when: false
  tags: sshkeys
