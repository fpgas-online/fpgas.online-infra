---
# Install zsh and tmux

- name: Install packages
  ansible.builtin.apt:
    name:
      - tmux
      - zsh
  tags:
    - tmux

# Copy zprofile to ~/.zprofile
# Copy tmux.conf to ~/.tmux.conf

- name: Copy zsh and tmux dotfiles to the pi user
  ansible.builtin.copy:
    src: "files/tmux/{{ item }}"
    dest: "/home/pi/.{{ item }}"
    mode: u+x
  with_items:
    - zshrc
    - zprofile
    - tmux.conf
  tags:
    - tmux

# Set shell for pi user to zsh
- name: Set shell to zsh
  ansible.builtin.user:
    name: pi
    shell: /usr/bin/zsh
  tags:
    - tmux
