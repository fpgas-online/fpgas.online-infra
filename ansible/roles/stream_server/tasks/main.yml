---
- name: Install the nginx RTMP base configuration
  ansible.builtin.include_tasks: base.yml
  tags:
    - streaming

- name: Configure the nginx streaming back end
  ansible.builtin.include_tasks: back.yml
  tags:
    - streaming
