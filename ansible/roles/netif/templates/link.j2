# {{ ansible_managed }}
# MAC-matched name for {{ item.name }} (Type=ether keeps VLAN children, which
# inherit the parent's MAC, from matching).
[Match]
MACAddress={{ item.mac | lower }}
Type=ether

[Link]
Name={{ item.name }}
